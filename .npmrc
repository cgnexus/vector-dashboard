# pnpm configuration
# Use faster symlinks and store dependencies efficiently
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true

# Enable faster installs
prefer-frozen-lockfile=true

# Store config
store-dir=~/.pnpm-store
